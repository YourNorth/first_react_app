import React from "react";
import ss from "./Header.module.css"

const Header = () => {
    return (
        <div className={ss.header}>
            It s header!
        </div>
    );
}

export default Header;